# 开发与测试依赖（在 requirements.txt 之上追加）
-r requirements.txt

# 测试框架
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.25
pytest-cov>=4.1,<6.0

# 属性测试（PBT）
hypothesis>=6.98,<7.0

# 代码质量
ruff>=0.3,<0.7
mypy>=1.8,<2.0
