"""核心基础工具占位包。

后续任务在此实现：
- ``config.py``        —— pydantic-settings 配置加载（任务 2 起逐步补齐）
- ``database.py``      —— SQLAlchemy engine / session（任务 2.2）
- ``security.py``      —— JWT / 密码学工具（任务 3）
- ``vault.py``         —— AES-256-GCM 凭证保险库（任务 4.1）
- ``audit_chain.py``   —— canonical_json + sha256 哈希链（任务 7.1）
"""
